# Virtual environments
.venv/
venv/

# Python
__pycache__/
*.pyc
*.pyo

# Secrets — never commit the API key
.env

# Empirical evaluation / ablation scripts — NOT part of the public software artifact
# (they require a private baseline corpus; the evaluation is reported in companion work).
research/

# Build artifacts
build/
dist/
*.egg-info/

# Generated / reproducible data
data/chroma/
data/*.json
*.log

# ...but the bundled sample corpus IS tracked (needed to run on a fresh clone)
!data/sample/
!data/sample/**

# Operating system
.DS_Store
Thumbs.db
.ipynb_checkpoints/
